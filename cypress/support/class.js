class Item {
    constructor(Id, IPUcode, Description, PackSize, PackType) {
      this.id = Id;
      this.ipucode = IPUcode;
      this.description = Description;
      this.packsize = PackSize;
      this.packtype = PackType;
    }
  }